const navigatorStyles = (theme) => ({
    appBar: {
        position: 'relative',
    },
    grow: {
        flexGrow: 1,
    },
    icon: {
        marginRight: theme.spacing.unit * 2,
    }
});

export default navigatorStyles;
